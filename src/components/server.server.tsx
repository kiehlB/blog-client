import styled from 'styled-components';

export type ServerProps = {};

function Server({}: ServerProps) {
  return (
    <div>
      <div>snippet</div>
    </div>
  );
}

const ServerBlock = styled.div``;

export default Server;
